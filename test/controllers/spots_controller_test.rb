# frozen_string_literal: true

require 'test_helper'

class SpotsControllerTest < ActionDispatch::IntegrationTest
  test 'spots controller will be tested later' do
    skip 'Devise 認証付きでのテストは後続Issueで実装予定'
  end
end
